{
  "name": "package1",
  "types": "index.d.ts",
  "dependencies": {
    "@types/node": "*",
    "typescript": "*",
    "package1": "file:../package1"

  },
  "scripts": {
    "pretest": "rm -rf dist && tsc",
    "test": "node dist/test.js"
  }
}